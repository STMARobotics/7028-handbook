% ============================================================
% 7028 Binary Battalion Handbook — LaTeX Template
% Matches 2nd Edition styling exactly
% ============================================================

\documentclass[12pt, letterpaper]{article}

% ── Packages ────────────────────────────────────────────────
\usepackage[
  top=1in, bottom=1in,
  left=1in, right=1in
]{geometry}

\usepackage{fontspec}           % XeLaTeX/LuaLaTeX font control
\usepackage{xcolor}
\usepackage{titlesec}           % Section heading formatting
\usepackage{titletoc}           % TOC formatting
\usepackage{fancyhdr}           % Headers and footers
\usepackage{graphicx}
\usepackage{booktabs}           % Nice tables
\usepackage{longtable}          % Multi-page tables
\usepackage{array}
\usepackage{colortbl}           % Colored table cells
\usepackage{parskip}            % Space between paragraphs
\usepackage{setspace}
\usepackage{ragged2e}           % Justification control
\usepackage{mdframed}           % Callout boxes
\usepackage{enumitem}           % List formatting
\usepackage{hyperref}
\usepackage{microtype}          % Better justification
\usepackage{pdfpages}           % Include full-page images
\usepackage{afterpage}
\usepackage{tcolorbox}          % Styled boxes
\tcbuselibrary{skins, breakable}
\usepackage{tabularx}
\usepackage{float}
\usepackage{wrapfig}

% ── Brand Colors ─────────────────────────────────────────────
\definecolor{BattalionBlue}{HTML}{2E4DA6}
\definecolor{BattalionGold}{HTML}{F5C400}
\definecolor{CalloutBg}{HTML}{F5F0DC}
\definecolor{CalloutBorder}{HTML}{C8BB8A}
\definecolor{TableBorder}{HTML}{AAAAAA}
\definecolor{BodyText}{HTML}{1A1A1A}

% ── Fonts ────────────────────────────────────────────────────
% Lora — exact match to original handbook (Google Font, serif)
% Static instances extracted from variable font by build pipeline
% (see build/build.sh and .github/workflows/handbook-build.yml)
\setmainfont{Lora}[
  Path           = fonts/,
  UprightFont    = Lora-Regular.ttf,
  BoldFont       = Lora-Bold.ttf,
  ItalicFont     = Lora-Italic.ttf,
  BoldItalicFont = Lora-BoldItalic.ttf,
]

% ── Body Text ─────────────────────────────────────────────────
\color{BodyText}
\setlength{\parindent}{0pt}
\setlength{\parskip}{6pt}
\justifying

% ── Heading Styles ───────────────────────────────────────────
% H1 — Blue, large (section titles like "Preface", "Policies")
\titleformat{\section}
  {\fontsize{22}{26}\selectfont\color{BattalionBlue}}
  {}{0em}{}
\titlespacing{\section}{0pt}{18pt}{10pt}

% Same style for unnumbered \section* (used in history year headings)
\titleformat{name=\section,numberless}
  {\fontsize{22}{26}\selectfont\color{BattalionBlue}}
  {}{0em}{}
\titlespacing*{\section}{0pt}{18pt}{10pt}

% H2 — Black, bold (subsection titles like "What is a platoon?")
\titleformat{\subsection}
  {\fontsize{12}{16}\selectfont\bfseries\color{BodyText}}
  {}{0em}{}
\titlespacing{\subsection}{0pt}{14pt}{4pt}

% H3 — Black, italic bold (used sparingly)
\titleformat{\subsubsection}
  {\fontsize{11}{14}\selectfont\bfseries\itshape\color{BodyText}}
  {}{0em}{}
\titlespacing{\subsubsection}{0pt}{10pt}{2pt}

% ── Table of Contents ────────────────────────────────────────
\titlecontents{section}
  [0pt]
  {\bfseries\vspace{6pt}}
  {}{}
  {\titlerule*[6pt]{.}\contentspage}

\titlecontents{subsection}
  [16pt]
  {\itshape}
  {}{}
  {}  % No page numbers for subsections in TOC (matches original)

% ── Page Headers/Footers ─────────────────────────────────────
\pagestyle{fancy}
\fancyhf{}
\renewcommand{\headrulewidth}{0pt}
\fancyfoot[C]{\thepage}

% Title pages and part dividers: no page number
\fancypagestyle{plain}{
  \fancyhf{}
  \renewcommand{\headrulewidth}{0pt}
}

% ── Callout Box ──────────────────────────────────────────────
% Matches the cream dashed-border boxes in the original
\tcbset{
  callout/.style={
    enhanced,
    breakable,
    colback=CalloutBg,
    colframe=CalloutBorder,
    boxrule=0pt,
    leftrule=0pt,
    rightrule=0pt,
    toprule=0pt,
    bottomrule=0pt,
    sharp corners,
    borderline north={0.5pt}{0pt}{CalloutBorder, dashed},
    borderline south={0.5pt}{0pt}{CalloutBorder, dashed},
    left=10pt, right=10pt, top=8pt, bottom=8pt,
    fontupper=\itshape,
  }
}

% Usage in markdown via raw LaTeX block:
% \begin{tcolorbox}[callout]
% Quote text here
% \end{tcolorbox}

% ── Design Strategy Box ──────────────────────────────────────
% Larger cream box used for the Design Strategy section
\tcbset{
  strategybox/.style={
    enhanced,
    breakable,
    colback=CalloutBg,
    colframe=CalloutBorder,
    boxrule=0pt,
    leftrule=0pt,
    rightrule=0pt,
    toprule=0pt,
    bottomrule=0pt,
    sharp corners,
    borderline north={0.5pt}{0pt}{CalloutBorder, dashed},
    borderline south={0.5pt}{0pt}{CalloutBorder, dashed},
    left=14pt, right=14pt, top=12pt, bottom=12pt,
  }
}

% ── Part Divider Page ────────────────────────────────────────
% Usage: \partdivider{assets/part1-icon.png}{Welcome to 7028}
\newcommand{\partdivider}[2]{
  \clearpage
  \thispagestyle{empty}
  \vspace*{\fill}
  \begin{center}
    \includegraphics[width=2.5in]{#1}\\[24pt]
    {\fontsize{26}{32}\selectfont\bfseries #2}
  \end{center}
  \vspace*{\fill}
  \clearpage
}

% ── Cover Page ───────────────────────────────────────────────
% Defined in sections/00-cover.md via raw LaTeX

% ── Hyperlink Styling ────────────────────────────────────────
\hypersetup{
  colorlinks=true,
  linkcolor=BattalionBlue,
  urlcolor=BattalionBlue,
  pdfborder={0 0 0}
}

% ── Table Styling ────────────────────────────────────────────
% Matches the minimal-border tables in competition history
\setlength{\tabcolsep}{8pt}
\renewcommand{\arraystretch}{1.4}


% ── Pandoc-required macros ───────────────────────────────────
\providecommand{\tightlist}{%
  \setlength{\itemsep}{2pt}\setlength{\parskip}{0pt}}
% ── History year heading (bypasses \section entirely) ───────
\newcommand{\yearheading}[1]{%
  {\fontsize{22}{26}\selectfont\color{BattalionBlue}\noindent #1}\par
  \vspace{2pt}%
}

% ── Document Body (required by Pandoc) ───────────────────────
\begin{document}

% TOC is generated inline via sections/02-toc.md

$body$

\end{document}
